import React from 'react';
import Layout from '../components/Layout';
import IRApplications from '../components/IRApplications';

const IRApplicationsPage = () => {
    return (
        <Layout>
            <IRApplications />
        </Layout>
    );
};

export default IRApplicationsPage; 